using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponSwap : PlayerMovement
{
    // Start is called before the first frame update
    void Start()
    {
        // on click, swap array to player holding other weapon and activate box around weapon
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void FixedUpdate() {
        
    }
}
